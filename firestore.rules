rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Recipes: public read, authenticated writes
    match /recipes/{recipeId} {
      allow read: if true;
      allow create, update: if request.auth != null;
    }
  }
}
