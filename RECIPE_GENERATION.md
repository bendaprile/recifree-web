# Recipe Generation Process & Prompt

Moved. The canonical version is `.agent/workflows/recipe.md`, and Claude Code exposes it as `/recipe`.

This file held a near-copy of that workflow. Two documents describing one process drift, and these two had: the workflow knows that recipes live in Firestore and that a JSON file alone renders nothing, and this one did not.
